# سوران دنتال — v28

تاريخ: 04-05-2026

## 🎯 الجديد في v28 (يحلّ مشكلة "البيانات الفارغة على دومين جديد")

### ١. مهلة المزامنة موسّعة
- من ٣ ثوانٍ إلى **١٠ ثوانٍ**
- يكفي حتى مع شبكات بطيئة (4G داخل العيادة، WiFi مزدحم)

### ٢. مزامنة خلفية ثانية بعد ٥ ثوانٍ
- التطبيق يُكمل المزامنة في الخلفية بعد فتح الواجهة
- **يُعيد رسم الصفحة الحالية تلقائياً** عند انتهاء المزامنة (لا حاجة لـ F5)
- إشعار "✅ تمت المزامنة من السحابة" يظهر عند النجاح

### ٣. زر "إعادة المزامنة الآن" 🔄 يدوي
- في **الإعدادات → ☁️ المزامنة مع السحابة**
- مفيد عند فتح التطبيق على جهاز/متصفح/Worker جديد
- يُظهر شريط حالة:
  - ⏳ جاهز
  - 🔄 جلب البيانات... (أثناء المزامنة)
  - ✅ تمت في 4.2ث — 152 مريض • 87 موعد • 23 خطة • 65 دفعة (نجاح، مع إحصائيات)
  - ❌ فشلت المزامنة: ... (فشل، مع السبب)

## 📋 كل ميزات v27 محفوظة

- إصلاح bug recursion للثلاث دوال (renderPage, renderDash, renderMyFile)
- رقم 07810151042 افتراضي + توقيع تلقائي بكل رسالة
- زر تعديل الموعد ✏️ مع رسالة WA لسبب التعديل
- تذكير دين تلقائي عند إكمال خطة بدين
- السعر تقديري→نهائي عند فتح الجلسة الأولى
- Browser Notifications + وميض جرس
- تعتيم الشاشة ٣٠ث أثناء استماع القرآن + Wake Lock
- ساعات الحجز ٩ ص–٩ م + فارق ٢٥ دقيقة
- ٢٠ مقالاً طبياً للمكتبة

## 🚀 خطوات النشر على Cloudflare Pages

1. فك ضغط `soran_dental_v28_split.zip` على جهازك
2. ادخل https://dash.cloudflare.com → Workers & Pages → **Create**
3. اختر **Pages** → **Upload assets**
4. اسحب المجلد المفكوك (الذي فيه index.html + app.js + app.css)
5. أعطِ المشروع اسماً (مثلاً `soran-v28`)
6. **Deploy**
7. افتح الرابط الجديد في متصفح **خاص (Incognito)**
8. سجّل دخول بحساب المالك
9. **إذا ظهرت البيانات في أول ١٠ ثوانٍ** → ممتاز، لا تفعل شيئاً
10. **إذا ظهرت "لا يوجد مرضى"** → اذهب إلى الإعدادات → ☁️ المزامنة مع السحابة → اضغط **🔄 إعادة المزامنة الآن** → انتظر الرسالة الخضراء

## 🛡️ شبكة الأمان

- البيانات في فايربيز (`mydent-9fb95` / `clinicId: soran_main`) لا تُلمَس أبداً
- يمكن العودة لأي إصدار سابق عبر Cloudflare → Deployments → Rollback
- النسخة الاحتياطية JSON دائماً متاحة من زر "📥 تصدير نسخة احتياطية كاملة"
